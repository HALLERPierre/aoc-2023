pub fn puzzle1() {
    // INPUT
    //     .split("\n\n")
    //     .filter(|line| line.len() > 0)
    //     .map(|packets_pair| {
    //         let packets = packets_pair.split("\n").collect::<Vec<&str>>();
    //         let packet1 = packets[0].chars().collect::<Vec<char>>();
    //         let packet2 = packets[1].chars().collect::<Vec<char>>();
    //         let mut i: usize = 0;

    //         loop {
    //             let (packet1_value, packet2_value) = (packet1[i], packet2[i]);

    //             match (packet1_value, packet2_value) {
    //                 ('[', '[') => i += 1,
    //                 _ => panic!("Unknown values {}, {}", packet1_value, packet2_value),
    //             }
    //         }
    //     });
}

pub fn puzzle2() {}
